package db.operation.hib;

import db.entity.Position;

/**
 * Interface only for separate operations in this case only for positions
 * management.
 * 
 * @author Mateusz Mucha
 *
 */
public interface PositionOperation extends HibernateOperation<Position> {

}